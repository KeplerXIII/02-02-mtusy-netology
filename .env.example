# Скопируйте в файл .env в корне проекта (`.env` в .gitignore).
# Скрипт и pytest подхватывают его через python-dotenv.
DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
